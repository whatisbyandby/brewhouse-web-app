import PleaseSignIn from "../../components/PleaseSignIn";

const Settings = props => {
  return (
    <div>
      <PleaseSignIn>
        <h2>Settings</h2>
      </PleaseSignIn>
    </div>
  );
};

export default Settings;
