# import * from './generated/prisma.graphql'

type Mutation {
  createRecipe(
    name: String
    type: RecipeType
    style: String
    batchSize: Float
  ): Recipe!
}

type Query {
  recipes: [Recipe]!
  recipe(where: RecipeWhereUniqueInput!): Recipe
}
